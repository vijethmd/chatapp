<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { title: 'Welcome to ChatApp' }) %>
<body class="d-flex flex-column justify-content-center align-items-center vh-100 bg-light">
  <div class="text-center">
    <h1 class="display-3 fw-bold mb-4 text-primary">💬 ChatApp</h1>
    <p class="lead mb-2">Connect, Chat and Share Messages with your friends instantly!</p>

    <p class="mb-5">
  Currently <span class="fw-bold text-primary"><%= totalUsers %></span> user<%= totalUsers !== 1 ? 's' : '' %> have joined
</p>

    <div class="d-flex justify-content-center gap-3">
      <a href="/signup" class="btn btn-lg btn-success px-5 py-2 shadow">Join Us</a>
      <a href="/login" class="btn btn-lg btn-outline-primary px-5 py-2 shadow">Login</a>
    </div>
  </div>
</body>
</html>
